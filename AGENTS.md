# AI Agent 開發指南與專案導覽

本專案是一個基於 C# / .NET 開發的 ImTK (ImGui Wrapper) 框架。
本文件 (`AGENTS.md`) 為 AI Agent 參與開發與維護時的**最高指導原則**與**專案導覽**。

---

## 1. 核心開發規範

在修改或重構任何程式碼之前，你**必須**遵守以下開發規範：

### 1.1 雜項
* **建置規範** : 在做完任何更動後，除非使用者要求，否則不需要進行 `dotnet build` 等建置動作，告知使用者修改完畢以及任務總結即可，編譯及測試交由使用者進行。
* **編輯規範** : 除非是單純的編譯或硬性邏輯錯誤，否則禁止在未取得使用者同意前開始修改，應盡可能和使用者將細節完整釐清。

### 1.2 任務探勘與討論原則
* **深度探勘為先**：在開始規劃具體的實作計畫之前，必須與使用者進行深度的討論以釐清需求與架構細節。
* **條列式討論**：與使用者討論時，請先條列式地列出所有需要討論的架構點或問題。
* **逐項深入**：接下來必須**一項一項**地深入討論，直到該項細節完全釐清並定案為止。**嚴禁將多個複雜的技術細節混雜在一起**大致帶過，以免造成架構理解上的模糊與未來重工。

### 1.3 任務追蹤與文檔維護原則
* **優先查看 `TODO.md`**：當收到新任務或指令不明確時，**必須**優先查看 `ImTK/docs/Project/TODO.md`，以確認當前的開發焦點。
* **找尋技術資料的途徑**：請透過查閱 `ImTK/docs/` 底下對應 Subsystem 資料夾（例如 `Core/`, `UI/`, `Database/` 等）中的 `README.md` 或架構文件來理解各模組的現行規範。
* **閱讀規格為先**：在修改任何模組的程式碼之前，必須先尋找並閱讀該模組相關的 Markdown 規格文件。
* **確認需求邊界**：如果使用者的要求涉及修改規格書「未提及」的部分，或是超出當前規格的範疇，必須先與使用者確認。
* **純粹的事實紀錄**：更新**技術規格文件**（`ImTK/docs/` 底下的所有 `.md`）時，只記錄事實與當下的架構現狀。絕對不要在這些文檔中加入修改日誌、更新歷史或 changelog。專案層級的 `CHANGELOG.md` 不在此限，應依照 Keep a Changelog 格式正常維護。

### 1.4 ImGui.NET 指標記憶體安全規範
因為 ImTK 封裝了 C++ 底層的 ImGui，在處理需要指標 (Pointers) 的操作時，**必須絕對避免因 C# 垃圾回收 (GC) 導致的懸空指標問題**。
* **規範**：當 AI 代理或開發者在呼叫 ImGui.NET 中任何需要寫入 `byte*` (字串指標) 的底層 API 時，**嚴禁使用 `fixed` 語句直接綁定 C# 字串陣列**。
* **解法**：**必須**使用 `Marshal.StringToCoTaskMemUTF8(string)` 將字串分配到不受 GC 管理的記憶體區塊中，然後強轉為指標傳給 ImGui。並有責任在物件生命週期結束（如 `OnClose`）時，呼叫 `Marshal.FreeCoTaskMem` 釋放該指標以防 Memory Leak。

### 1.5 C# 開發與 API 封裝習慣
* **Property Over Field**：影響功能狀態的變數（如 `enableXXX`、`isResizable`），必須實作為 C# Property，以便未來攔截 getter/setter 或綁定事件。
* **ImGui Flags 封裝**：不要讓最終開發者直接操作 `ImGuiWindowFlags` 等底層位元遮罩。應將其封裝為直覺的布林屬性 (如 `enableDocking`)，並在內部自動進行位元運算映射。

### 1.6 模組 (ImTKModule) 建構規範
* 所有的 `ImTKModule` 都是透過反射機制作全局單例初始化。
* **規範**：任何實作 `ImTKModule` 的類別，**必須擁有「唯一一個、無參數、且為非公開 (private/protected)」的建構函式**。若違反此規範，`ImTKApplication` 會在啟動時拋出 `InvalidOperationException` 並中斷程式。

### 1.7 腳本執行與檔案修改原則
* **不使用外部批次腳本替換**：在進行大規模程式碼修改時，**嚴禁**使用 `python` 或 `powershell` 等外部腳本進行批次讀寫替換，以免因環境沙盒掛載問題導致非預期的破壞。
* **使用內建讀寫機制**：請務必使用內建的讀取與寫入工具（如 `replace_file_content`），以確保檔案內容正確地被逐一更新與追蹤。


---

## 2. 專案子模組導覽

目前專案的核心邏輯被劃分為幾個主要的模組 (Subsystems)。
你可以從 `ImTK/docs/README.md` 開始瀏覽，或者直接進入各子目錄查閱文檔。

* **`Project`**：專案管理、開發進度與命名規範。
* **`Core`**：全域生命週期、模組 (`ImTKModule`)、物件 (`ImTKObject`) 與程式進入點 (`ImTKSilk`)。
* **`UI`**：視覺樹 (`VisualElement`) 與排版。
* **`Database`**：資源管理 (`ImTKDatabase`)。
* **`Log`**：日誌系統 (`ImTKLog`)。
* **`Event`**：事件與分派器 (`ImTKDispatcher`)。
